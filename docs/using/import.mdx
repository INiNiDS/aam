# Import Directive
You can import files using the `@import` directive:

```aam
@import file.aam
```
This will import all the contents of `file.aam` into the current file, allowing you to use any schemas, mappings, or other definitions from `file.aam` in your current configuration. You can also import specific items from a file using the `@derive` directive, which allows you to create instances of schemas defined in another file. For example:
